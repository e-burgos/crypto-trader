# FIX-e-burgos-031 — 2026-09-04

## Estructura

- `buildAnalysisPrompt` se retiró de `src/lib/llm/llm-types.ts` y del barrel `src/lib/llm/index.ts`:
  era código muerto (sólo lo usaba su propio test; `apps/api` arma el prompt por otro camino).
  `parseLLMResponse`, `LLMDecision` y los providers no cambian.
