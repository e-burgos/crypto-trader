# trading-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build trading-engine` to build the library.

## Running unit tests

Run `nx test trading-engine` to execute the unit tests via [Vitest](https://vitest.dev/).
