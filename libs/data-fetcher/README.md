# data-fetcher

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-fetcher` to build the library.

## Running unit tests

Run `nx test data-fetcher` to execute the unit tests via [Vitest](https://vitest.dev/).
