# Context Prompt — apps/api-e2e

> Entry point para agentes que trabajen sobre `apps/api-e2e`.
> Leer junto con `constitution.md` de este directorio **+ `updates/*.md` en orden de nombre**.

- **Tipo:** app
- **Estado:** proyecto pre-existente adoptado por el arnés SDD — sin ciclos SDD completados todavía.
- Cómo correr: `pnpm nx e2e api-e2e` (levanta/apunta al API local; requiere postgres+redis de `pnpm docker:infra`).
