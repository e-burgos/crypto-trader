# Base stage
FROM node:22-alpine AS base
RUN npm install -g pnpm@9
WORKDIR /app

# Dependencies stage — apps/api has no package.json, all deps live at root
FROM base AS deps
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
RUN pnpm install --frozen-lockfile

# Builder stage
FROM deps AS builder
COPY . .
# Generate Prisma client before building (DATABASE_URL needed by prisma.config.ts loader)
RUN cd apps/api && DATABASE_URL=postgresql://dummy:dummy@localhost/dummy \
    ../../node_modules/.bin/prisma generate
# Build API — output goes to dist/apps/api/ (see webpack.config.js)
RUN pnpm nx build api --skip-nx-cache

# Production stage — lean image using package.json generated by webpack (generatePackageJson: true)
FROM node:22-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production

# dist/apps/api/package.json is auto-generated by NxAppWebpackPlugin with prod deps only
COPY --from=builder /app/dist/apps/api/package.json ./package.json
RUN npm install --omit=dev

# Copy compiled bundle
COPY --from=builder /app/dist/apps/api ./dist/apps/api
# Copy Prisma schema & migrations (needed for prisma migrate deploy at startup)
COPY --from=builder /app/apps/api/prisma apps/api/prisma

EXPOSE 3000

CMD ["node", "dist/apps/api/main.js"]
