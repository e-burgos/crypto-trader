# Context Prompt — apps/web-e2e

> Entry point para agentes que trabajen sobre `apps/web-e2e`.
> Leer junto con `constitution.md` de este directorio **+ `updates/*.md` en orden de nombre**.

- **Tipo:** app
- **Estado:** proyecto pre-existente adoptado por el arnés SDD — sin ciclos SDD completados todavía.
- Cómo correr: `pnpm nx e2e web-e2e` (Playwright; el dev server de web se levanta según config).
